package main

import "strings"

func Test(str1, str2 string, len1, len2 int) string {
	if len1 > len2 {

	}
}

func main() {
	var length1, length2 int
	var str1_old, str2_old string
	str1_new := strings.Replace(str1_old, " ", "", -1)
	str2_new := strings.Replace(str2_old, " ", "", -1)
}
