package main

import "fmt"

func main() {
	var n, p int
	fmt.Scanf("%d %d", &n, &p)
	Arr := make([]int, n)
	daoju := make([]int, 3)
	for i := 0; i < n; i++ {

		fmt.Scanf("%d %d %d ", &Daoju{})
		Arr = append(Arr, daoju...)
	}

}

type Daoju struct {
	count int
	price int
	value int
}
